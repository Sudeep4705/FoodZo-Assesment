# 🍽️ Foodzo
A full-stack web application built using **React (TypeScript) + Node.js + Express + MongoDB + JWT + Cloudinary**.
## 🖥️ Backend Setup (Node.js + TypeScript)

Run the following commands:

```sh
cd backend
npm install
npm install -D typescript ts-node nodemon @types/node @types/express
npx tsc --init
npm run dev

FRONTEND SETUP (React + TypeScript)
cd frontend
npm install
npm run dev


To access the Admin page, navigate to:
/admin 



